#include"stdio.h"
#include "regist.h"

// Patients already registered list is given as input for verification
// v stands for patient details
int existregist(){
    v[1].age=123;
    v[1].aadhar=123;
    v[1].phone_number=123;
    v[1].secret_code=123;
    v[1].vaccine_type=1;
    v[1].vaccine_doses=1;
    return 0;
}
